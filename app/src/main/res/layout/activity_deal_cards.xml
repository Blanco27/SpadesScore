<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="16dp"
    android:paddingBottom="24dp"
    android:background="?attr/appBg"
    tools:context=".DealCardsActivity">

    <!-- Top score strip: brand row + per-player scores -->
    <include
        layout="@layout/include_round_score_strip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Gap: sets the score strip apart from the round block (mockup grouping) -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.9" />

    <!-- Round title (number tinted accent in code) -->
    <TextView
        android:id="@+id/round_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.Spades.Title" />

    <!-- Progress bar -->
    <ProgressBar
        android:id="@+id/round_progress_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:progressDrawable="@drawable/progress_track"
        android:layout_marginTop="12dp" />

    <!-- Dealer subtitle: e.g. "Anna deals" / "Anna teilt aus" -->
    <TextView
        android:id="@+id/dealer_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.Spades.Body"
        tools:text="Anna deals" />

    <!-- Gap before the card illustration -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1.1" />

    <!-- Deal illustration: card fan + card count + label (all centered) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center">

        <ImageView
            android:layout_width="160dp"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:src="@drawable/ic_card_fan"
            android:contentDescription="@string/image_of_playing_cards" />

        <!-- Number + "cards per player" label, baseline-aligned side by side -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:baselineAligned="true"
            android:layout_marginTop="10dp">

            <TextView
                android:id="@+id/card_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="@style/TextAppearance.Spades.Number"
                android:textSize="40sp"
                android:textColor="?attr/appAccent"
                tools:text="5" />

            <TextView
                android:id="@+id/cards_per_player_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="7dp"
                android:textAppearance="@style/TextAppearance.Spades.Body"
                tools:text="cards per player" />

        </LinearLayout>

    </LinearLayout>

    <!-- Bottom spacer -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- CTA button -->
    <Button
        android:id="@+id/deal_next_button"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@drawable/bg_cta"
        android:stateListAnimator="@null"
        android:elevation="@dimen/cta_elevation"
        android:outlineAmbientShadowColor="@color/cta_shadow"
        android:outlineSpotShadowColor="@color/cta_shadow"
        android:text="@string/continue_label"
        android:textColor="?attr/appCtaFg"
        android:fontFamily="@font/space_grotesk_semibold"
        android:textAllCaps="false"
        android:textSize="16sp"
        app:backgroundTint="@null" />

</LinearLayout>
