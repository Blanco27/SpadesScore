<?xml version="1.0" encoding="utf-8"?>
<!--
  Shared round header for Declare / Confirm screens.
  Score strip (brand + per-player scores) + round title (number tinted accent in code)
  + progress bar. The Deal screen composes the strip and title separately so it can
  space them apart (mockup grouping). Bind via Activity.bindRoundHeader(...) in ui/RoundHeader.kt.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Brand row + per-player score row -->
    <include
        layout="@layout/include_round_score_strip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Round title (number tinted accent in code) -->
    <TextView
        android:id="@+id/round_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.Spades.Title"
        android:layout_marginTop="38dp" />

    <!-- Progress bar -->
    <ProgressBar
        android:id="@+id/round_progress_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:progressDrawable="@drawable/progress_track"
        android:layout_marginTop="12dp" />

</LinearLayout>
